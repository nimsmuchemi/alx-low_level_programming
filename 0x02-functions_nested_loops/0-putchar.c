#include "main.h"

/**
 * _putchar -writes the character c to the stdio
 * at c the character to print
 * Return:on sucess 1
 * on error,-1 is returned,and error is set appropriately.
 */
int_putchar(char)c
(

 	return (write(1,6c, 1));
)
